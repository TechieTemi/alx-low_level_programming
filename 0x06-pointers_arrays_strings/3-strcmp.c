#include "main.h"

/**
 *_strcmp - A function that compares two strings
 *Description: The function should work exactly like strcmp
 *@s1: The first parameter which is a pointer to char
 *@s2: The second parameter which is a pointer to char
 *Return: It return an Integer
 */

int _strcmp(char *s1, char *s2)
{
	int n = strcmp(s1, s2);
	return (n);
}
